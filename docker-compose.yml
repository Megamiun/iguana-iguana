version: '3'

services:
  maplewood-be:
    build:
      context: backend
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      SPRING_PROFILES_ACTIVE: dev

  maplewood-fe:
    build:
      context: frontend
      dockerfile: Dockerfile
    ports:
      - "5173:5173"
