version: '3'

services:
  maplewood-be:
    build:
      context: backend
      dockerfile: Dockerfile
    ports:
      - "8080:8080"

  maplewood-fe:
    build:
      context: frontend
      dockerfile: Dockerfile
    ports:
      - "5173:5173"
